# Example nixpacks.toml for a Node.js application
[phases.build]
cmds = ["npm install", "npm run build"]

[start]
cmd = "npm start"

[providers]
include = ["nodejs", "..."]

[variables]
NIXPACKS_NODE_VERSION = "20"