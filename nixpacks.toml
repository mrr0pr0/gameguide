# More reliable configuration
[phases.setup]
nixPkgs = ["nodejs", "yarn"]
aptPkgs = ["git"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"

[variables]
NIXPACKS_NODE_VERSION = "20"